
1，发布版本在package下，把下面的S5K3L8.ini文件移动到f:/下，解压package下的压缩包,运行HSFoxx.exe即可;
2，若要运行工程：
	a,解压缩opencv2410.zip到当前文件夹；
	b,解压activealignmentlib\联调code\distribute\include.rar到当前文件夹；
	c,把include的库文件放到debuu32目录下(这里只提供了debug32的库文件)；
	d,运行DeviceManager.sln；



就